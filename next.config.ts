import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["img.freepik.com", "github.com", "upload.wikimedia.org", "cdn-icons-png.freepik.com"],
  },
};

export default nextConfig;
